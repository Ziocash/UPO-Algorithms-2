{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "java",
            "targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
            "elements": [
                "${compileOutput}",
                "${dependencies}",
                "${workspaceFolde}"
            ],
            "problemMatcher": [],
            "label": "Export jar with source code"
        }
    ]
}